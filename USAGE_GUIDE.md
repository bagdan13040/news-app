# 📱 Руководство по использованию News App

## 🚀 Быстрый старт

### 1. Настройка API ключей

#### Получение OpenRouter API ключа (для факт-чекинга и синонимов)

1. Перейдите на [openrouter.ai](https://openrouter.ai/)
2. Зарегистрируйтесь или войдите
3. Перейдите в раздел **Keys** в личном кабинете
4. Создайте новый API ключ
5. Скопируйте ключ

#### Настройка .env файла

Откройте файл `.env` в корне проекта и вставьте ваш ключ:

```env
OPENROUTER_API_KEY=sk-or-v1-ваш-ключ-здесь
```

> ⚠️ **Важно**: Не загружайте .env с реальным ключом в публичный репозиторий!

---

## 💻 Запуск на компьютере (тестирование)

### Требования
- Python 3.10+
- Windows/Linux/macOS

### Установка зависимостей

```bash
# Создайте виртуальное окружение
python -m venv .venv

# Активируйте его
# Windows:
.\.venv\Scripts\activate
# Linux/macOS:
source .venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### Запуск приложения

```bash
python kivy_news_app.py
```

Приложение откроется в окне размером 375x667 (размер iPhone).

---

## 📦 Сборка APK для Android

### Вариант 1: Автоматическая сборка через GitHub Actions ⭐ (рекомендуется)

#### Преимущества:
- ✅ Не нужно настраивать локальное окружение
- ✅ Сборка происходит автоматически
- ✅ Работает на любой ОС
- ✅ Готовый APK через 15-20 минут

#### Как использовать:

1. **Убедитесь, что код загружен на GitHub** (уже сделано ✓)

2. **Перейдите во вкладку Actions**
   - Откройте ваш репозиторий: https://github.com/bagdan13040/news-app
   - Перейдите во вкладку **Actions**

3. **Дождитесь завершения сборки**
   - После каждого push автоматически запускается workflow
   - Процесс занимает 15-20 минут
   - Зелёная галочка = сборка успешна

4. **Скачайте APK**
   - Нажмите на завершённый workflow
   - Внизу страницы в разделе **Artifacts** найдите `news-app-apk`
   - Скачайте ZIP архив и распакуйте
   - Внутри будет файл `newsearch-1.0-armeabi-v7a_arm64-v8a-debug.apk`

5. **Установите на телефон**
   - Перенесите APK на Android устройство
   - Включите установку из неизвестных источников (Settings → Security)
   - Откройте APK и установите

---

### Вариант 2: Локальная сборка (для опытных пользователей)

#### Windows (через WSL2)

**Требования:**
- Windows 10/11
- WSL2 установлен
- Ubuntu 22.04 в WSL2

**Пошаговая инструкция:**

```bash
# 1. Откройте WSL2 (Ubuntu)
wsl

# 2. Перейдите в папку проекта
cd /mnt/e/news_final

# 3. Установите зависимости
sudo apt update
sudo apt install -y git zip unzip openjdk-17-jdk wget autoconf libtool pkg-config \
    zlib1g-dev libncurses5-dev libncursesw5-dev libtinfo5 cmake libffi-dev \
    libssl-dev python3 python3-pip

# 4. Установите buildozer
pip3 install --upgrade buildozer cython

# 5. Установите Android SDK и NDK
export ANDROIDAPI=33
export NDKAPI=21
buildozer android debug

# Первая сборка займёт 30-60 минут!
# APK будет в папке: bin/newsearch-1.0-armeabi-v7a_arm64-v8a-debug.apk
```

#### Linux / macOS

Смотрите подробную инструкцию в файле [ANDROID_BUILD.md](ANDROID_BUILD.md).

---

## 📱 Установка на Android

### Требования
- Android 5.0+ (API 21+)
- ~100 MB свободного места
- Разрешение на установку из неизвестных источников

### Установка

1. **Включите установку из неизвестных источников**
   - Откройте **Настройки** → **Безопасность**
   - Включите **Неизвестные источники** или **Установка из неизвестных источников**
   - Для Android 8+: разрешите установку для конкретного приложения (например, Chrome или Файлы)

2. **Перенесите APK на телефон**
   - USB кабель → скопируйте в Downloads
   - Облако (Google Drive, Dropbox)
   - Email себе и скачайте на телефоне

3. **Установите**
   - Откройте файловый менеджер
   - Найдите APK в папке Downloads
   - Нажмите на файл
   - Подтвердите установку

4. **Запустите приложение**
   - Иконка **NewsSearch** появится в списке приложений

---

## 🎯 Функционал приложения

### Главный экран (Home)

**1. Погодный виджет**
- Показывает текущую температуру для Уфы (54.74, 55.97)
- Состояние погоды, скорость ветра, влажность
- Обновляется каждые 5 минут

**2. Биржевые данные**
- 10 инструментов: USD, EUR, NVIDIA, Apple, Google, Microsoft, Tesla, Amazon, Bitcoin, Ethereum
- Цены в долларах США
- Изменение за день в процентах (цвет: зелёный рост, красный падение)
- Свайп влево/вправо для просмотра всех карточек

**3. Трендовые новости**
- 30 самых популярных новостей из Google News
- Отображается по 2 новости
- Кнопки вверх/вниз для навигации
- Клик по новости → автоматический поиск по этой теме

**4. Категории новостей**
- 6 категорий: Технологии, Наука, Бизнес, Здоровье, Спорт, Развлечения
- Цветные карточки
- Клик по категории → поиск новостей по этой теме

### Поиск (Search)

**Расширенный поиск с синонимами:**
1. Введите запрос (например, "искусственный интеллект")
2. Система автоматически добавит синонимы через LLM
3. Резервный механизм через DuckDuckGo Autocomplete
4. Результаты из 100+ источников новостей

**Функции:**
- Клик по карточке → полный текст статьи
- Сохранение полного контента в кэше

### Чтение статьи

**Факт-чекинг (кнопка с иконкой щита):**
1. Нажмите кнопку факт-чека в верхнем правом углу
2. LLM анализирует статью на предмет:
   - Достоверности утверждений
   - Источников информации
   - Предвзятости
   - Манипуляций
3. Результат отображается в диалоговом окне

---

## ⚙️ Настройка под себя

### Изменение города для погоды

Откройте `kivy_news_app.py`, найдите строку:

```python
weather = backend.get_weather(54.74, 55.97)  # Уфа
```

Замените координаты на свои (можно найти на Google Maps).

### Добавление других акций

Откройте `backend.py`, найдите функцию `get_financial_data()`:

```python
default_symbols = [
    "RUB=X",      # USD/RUB
    "EURRUB=X",   # EUR/RUB
    "NVDA",       # NVIDIA
    # Добавьте свои тикеры здесь
]
```

Список популярных тикеров Yahoo Finance:
- `AAPL` - Apple
- `GOOGL` - Google
- `MSFT` - Microsoft
- `TSLA` - Tesla
- `AMZN` - Amazon
- `META` - Meta (Facebook)
- `NFLX` - Netflix
- `BTC-USD` - Bitcoin
- `ETH-USD` - Ethereum

### Изменение языка новостей

Откройте `backend.py`, найдите:

```python
url = f"https://news.google.com/rss?hl=ru&gl=RU"
```

Измените параметры:
- `hl=ru` → язык интерфейса (ru, en, de, fr и т.д.)
- `gl=RU` → регион (RU, US, GB, DE и т.д.)

---

## 🐛 Решение проблем

### Приложение не запускается на Android

**Проблема:** Чёрный экран или крэш при запуске

**Решение:**
1. Проверьте версию Android (минимум 5.0)
2. Убедитесь, что есть интернет-соединение
3. Дайте разрешения приложению:
   - Settings → Apps → NewsSearch → Permissions
   - Разрешите "Storage" и "Network"

### Факт-чекинг не работает

**Проблема:** "Ошибка проверки. Попробуйте позже"

**Решение:**
1. Проверьте наличие API ключа в `.env`
2. Убедитесь в правильности ключа (начинается с `sk-or-v1-`)
3. Проверьте баланс на OpenRouter.ai
4. Убедитесь в наличии интернета

### Синонимы не добавляются

**Проблема:** Поиск работает только по прямому запросу

**Решение:**
- Если нет API ключа, система использует DuckDuckGo Autocomplete (работает без ключа)
- Проверьте консоль на ошибки
- Система имеет таймаут 3 секунды, после чего использует оригинальный запрос

### Биржевые данные не загружаются

**Проблема:** Пустые карточки или "N/A"

**Решение:**
1. Проверьте интернет-соединение
2. Yahoo Finance может быть временно недоступен
3. Проверьте правильность тикеров в `backend.py`
4. Кэш обновляется каждые 5 минут

---

## 📊 Технические детали

### Архитектура

```
kivy_news_app.py          # Главное приложение (UI)
├── HomeScreen            # Главный экран с виджетами
├── SearchScreen          # Поиск новостей
└── ArticleScreen         # Чтение статьи

backend.py                # API интеграции
├── get_weather()         # open-meteo.com
├── get_financial_data()  # Yahoo Finance
└── get_google_trends()   # Google News RSS

llm_integration.py        # LLM функции
├── generate_related_keywords()  # Генерация синонимов
└── fact_check()          # Факт-чекинг статей

news_search_core.py       # Поиск новостей
└── DuckDuckGo + 100+ источников
```

### Кэширование

- **Погода**: 5 минут (300 сек)
- **Биржа**: 5 минут (300 сек)
- **Тренды**: 30 минут (1800 сек)
- **Синонимы**: 6 часов (21600 сек)

### Используемые API

| Сервис | Назначение | Требуется ключ |
|--------|-----------|----------------|
| open-meteo.com | Погода | ❌ Нет |
| Yahoo Finance | Акции/валюты | ❌ Нет |
| Google News RSS | Тренды | ❌ Нет |
| DuckDuckGo | Поиск новостей | ❌ Нет |
| OpenRouter AI | Факт-чекинг, синонимы | ✅ Да |

---

## 🔄 Обновление приложения

### Через GitHub Actions

1. Внесите изменения в код
2. Закоммитьте и запушьте на GitHub:
   ```bash
   git add .
   git commit -m "Описание изменений"
   git push
   ```
3. Дождитесь завершения сборки в Actions
4. Скачайте новый APK
5. Установите поверх старой версии

### Локально

```bash
# В WSL2/Linux
buildozer android debug

# APK будет обновлён в папке bin/
```

---

## 📝 Полезные ссылки

- **Репозиторий**: https://github.com/bagdan13040/news-app
- **Buildozer документация**: https://buildozer.readthedocs.io/
- **KivyMD компоненты**: https://kivymd.readthedocs.io/
- **OpenRouter AI**: https://openrouter.ai/
- **Yahoo Finance тикеры**: https://finance.yahoo.com/

---

## 💡 Советы по разработке

### Тестирование на компьютере

Всегда тестируйте изменения на компьютере перед сборкой APK:

```bash
python kivy_news_app.py
```

### Отладка на Android

Подключите устройство через USB и используйте logcat:

```bash
adb logcat | grep python
```

### Оптимизация сборки

Используйте кэширование в GitHub Actions (уже настроено):
- Первая сборка: ~20 минут
- Последующие: ~5-7 минут

---

## 🆘 Поддержка

Если возникли проблемы:

1. Проверьте логи в терминале
2. Изучите [ANDROID_BUILD.md](ANDROID_BUILD.md) для деталей сборки
3. Создайте Issue в GitHub репозитории
4. Проверьте наличие интернета и API ключей

---

## 📄 Лицензия

Проект использует открытые API и библиотеки. Убедитесь, что соблюдаете условия использования:
- OpenRouter API: проверьте лимиты и тарифы
- Yahoo Finance: только для личного использования
- Не злоупотребляйте частыми запросами к API

---

**Приятного использования! 🚀**
