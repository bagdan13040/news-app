# NewsSearch - Мобильное приложение для поиска новостей

Приложение для поиска и чтения новостей с интеграцией LLM (ChatGPT) на базе Kivy/KivyMD.

## 🚀 Возможности

- 🔍 Поиск новостей через DuckDuckGo
- 📰 Извлечение и отображение полного текста статей
- 🤖 Интеграция с ChatGPT для анализа и обработки новостей
- 🌤️ Погодная информация
- 💹 Финансовые данные
- 📊 Google Trends
- 📱 Адаптивный интерфейс для мобильных устройств

## 📦 Автоматическая сборка APK (GitHub Actions)

Проект настроен на автоматическую сборку APK через GitHub Actions.

### Настройка Secrets
Для корректной работы приложения (генерация синонимов через AI) необходимо добавить ключи API в Secrets репозитория:

1. Перейдите в **Settings** -> **Secrets and variables** -> **Actions**.
2. Нажмите **New repository secret**.
3. Добавьте:
   - `OPENAI_API_KEY`: Ваш ключ OpenAI (или OpenRouter).
   - `OPENROUTER_API_KEY`: (Опционально) Если используете OpenRouter.

### Запуск сборки
Сборка запускается автоматически при:
- Push в ветку `main` или `master`.
- Ручном запуске через вкладку **Actions** -> **Build Android APK** -> **Run workflow**.

### Скачивание APK
1. Перейдите на вкладку **Actions**.
2. Выберите последний успешный запуск workflow.
3. Внизу страницы в разделе **Artifacts** скачайте файл `news-search-apk`.

## 🛠️ Локальная сборка APK

### Требования

- Linux (Ubuntu/Debian рекомендуется)
- Python 3.10+
- Buildozer
- Android SDK и NDK

### Установка зависимостей

```bash
# Системные зависимости (Ubuntu/Debian)
sudo apt-get update
sudo apt-get install -y git zip unzip openjdk-17-jdk wget
sudo apt-get install -y autoconf automake libtool pkg-config
sudo apt-get install -y zlib1g-dev libncurses5-dev
sudo apt-get install -y cmake libffi-dev libssl-dev
sudo apt-get install -y libxml2-dev libxslt1-dev

# Python зависимости
pip install buildozer cython python-for-android
```

### Сборка

```bash
# Debug сборка
buildozer android debug

# Release сборка (требуется keystore)
buildozer android release
```

APK файл будет в папке `bin/`.

## 🔧 Настройка

### API ключи

Создайте файл `.env` в корне проекта:

```env
OPENAI_API_KEY=your_openai_api_key_here
```

## 📱 Установка на Android

1. Включите "Неизвестные источники" в настройках Android
2. Скачайте APK из Releases или Actions
3. Установите APK на устройство

## 🏗️ Структура проекта

```
news_final/
├── main.py                  # Точка входа для Buildozer
├── kivy_news_app.py        # Главное приложение Kivy
├── news_search_core.py     # Логика поиска новостей
├── news_parsing.py         # Парсинг статей
├── llm_integration.py      # Интеграция с OpenAI
├── backend.py              # Дополнительные API (погода, финансы)
├── buildozer.spec          # Конфигурация сборки Android
├── requirements.txt        # Python зависимости
└── .github/
    └── workflows/
        └── build-android.yml  # CI/CD конфигурация
```

## 🐛 Известные проблемы

- Для работы с OpenAI API требуется интернет-соединение
- Некоторые сайты могут блокировать парсинг контента
- Минимальная версия Android: 5.0 (API 21)

## 📄 Лицензия

MIT License

## 🤝 Вклад

Pull requests приветствуются! Для крупных изменений сначала откройте issue.

## 📞 Поддержка

Создайте [issue](../../issues) для вопросов и багов.
